name: Deploy
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: |
          cd /home/gshorti/Vibe_Code_Telegram_Mini_App_GZG_Backend/
          git pull
          docker compose up -d --build
